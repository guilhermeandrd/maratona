#include <bits/stdc++.h>

using namespace std;

int main() {

    ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    int n=0, m=0;

    cin >> n;
    cin >> m;

    vector<int> v;

    for(int i=0; i< m; i++){
        v.push_back(0);
    }

    while(n--){
        for(int i=0; i<m; i++){
            int e; cin>> e;
            if(e >                              )
        }
    }

    return 0;
}