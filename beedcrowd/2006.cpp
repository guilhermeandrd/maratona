#include <bits/stdc++.h>

using namespace std;

int main() {

    ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    int n=0; cin >> n;

    int i, s=0;

    while(cin >> i){
        if(i = n) s++;
    }

    cout << s << endl;

    return 0;
}